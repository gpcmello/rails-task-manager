<h2><%= @task.title %></h2>

<p><%= @task.details %></p>

<br>

<%= link_to "Back", tasks_path %>
<br>
<%= link_to "Edit task", edit_task_path(@task.id) %>
<br>
<%= link_to "Delete task", task_path(@task.id), data: {turbo_method: :delete, turbo_confirm: "Are you sure to remove #{@task.title}?"} %>
